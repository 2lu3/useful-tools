[project]
name = "audio-to-text"
version = "0.1.0"
description = "Transcribe audio files to text with OpenAI Speech-to-Text"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.99.0",
]

[dependency-groups]
dev = [
    "black>=25.9.0",
]
